rules_version = '2';

// RÈGLES TEMPORAIRES POUR DEBUG - NE PAS UTILISER EN PRODUCTION
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
